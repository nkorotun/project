import React from 'react';
import {Text, SafeAreaView} from 'react-native';

export const Camera = () => {
  return (
    <SafeAreaView>
      <Text>Welcome, to camera page.</Text>
    </SafeAreaView>
  );
};

import styled from 'styled-components/native';
import {COLORS} from '../../constants/colors';

export const ControlInputStyles = {
  Message: styled.Text`
    text-align: center;
    color: ${COLORS.red};
  `,
};
